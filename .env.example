PORT=8787
DATABASE_URL=postgres://prospector:prospector@localhost:5432/prospector
# Only needed if server/main.ts isn't finding dist-web next to itself (e.g. a custom layout)
# STATIC_ROOT=/path/to/dist-web
